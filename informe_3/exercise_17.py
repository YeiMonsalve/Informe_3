# Conversión de tipos en Python.
# Comparación de cadena - convierte a booleano - Boolean.parseBoolean.

activo = 'true'
valor = activo.lower() == 'true'

print(valor)

#bool("true") retorna True para cualquier cadena no vacía. La comparación anterior replica el comportamiento exacto de Java.